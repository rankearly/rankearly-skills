# Conversion Hooks (Subagent)

If seo-memory was loaded, scan it and pick pieces that naturally fit this article's topic and reader intent:

- **Offerings** — which capabilities/benefits are directly relevant to the reader's problem?
- **Pricing** — is there a free tier or low-commitment entry point worth mentioning?
- **Audience** — which segment matches the target reader? Use their pain points and goals to frame the narrative.
- **Positioning** — any differentiator that answers a question the reader would naturally ask?

Write a short selection (a few bullet points) into `{PROJECT_ROOT}/blogs/<topic>/conversion-hooks.md` — what you picked and where it fits in the article. For each hook, include the source URL so the outline and draft can create contextual internal links like `[link text](/source-url)`. These are not ads; they should feel like natural examples, proof points, or recommendations within the content.

If seo-memory was not loaded, skip this step.
