Elegant editorial-style documentation illustration on a pure white background. Landscape canvas sized to the actual footprint of the diagram with balanced padding on all sides, centered composition, no forced ratio beyond staying horizontal. Match the visual language of `assets/cluster-related-keywords.md`: crisp anti-aliased vector lines, Inter / SF Pro style sans-serif, dark charcoal text, very light gray borders, soft tidy shadows, restrained blue and teal accents, generous whitespace, and consistent rounded geometry.

Layout: show one refined left-to-right transformation from keyword clusters into broader topics. On the left, place five incoming keyword-cluster pills stacked vertically with generous spacing. In the center, place one compact NLP grouping block. On the right, place two separate topic containers stacked vertically. No shared outer frame around both topics. Keep the center block smaller than the topic side so the grouped result carries more weight.

LEFT SIDE — raw keyword clusters:
- Show five light neutral pills with the same softness and geometry as the input pills in the clustering example. Exact labels from top to bottom:
  1. welcome email sequence
  2. welcome email templates
  3. abandoned cart email
  4. email subject line formulas
  5. newsletter subject line tips
- Use a very soft neutral tint, delicate border, subtle shadow only if needed, and dark text. Do not pre-color these by topic.

CENTER — NLP grouping:
- Show one small rounded block labeled `NLP grouping`.
- Add one minimal clustering glyph inside it.
- Keep this block quiet, neutral, and precise. It is a transition step, not the hero object.

RIGHT SIDE — grouped topics:
- Build the right side using the same visual logic as the `RIGHT COLUMN — resulting clusters` section in `assets/cluster-related-keywords.md`.
- Each topic is one softly tinted rounded container.
- Inside each container, place one colored header pill at the top for the topic label, then lighter child pills beneath it.
- The header pill and child pills must all use the same rounded-rectangle shape language: same corner-radius family, same pill height family, same left and right inset, same visual alignment. Do not make the header a big capsule while the child pills become smaller chips.
- Topic 1 uses a pale blue container tint with a blue header pill labeled `welcome email sequence`. Beneath it, place two light neutral child pills labeled `welcome email templates` and `abandoned cart email`.
- Topic 2 uses a pale teal container tint with a teal header pill labeled `email subject line formulas`. Beneath it, place one light neutral child pill labeled `newsletter subject line tips`.
- The colored header pill should be visually distinct through fill color only. Do not change its geometry, size class, or corner style relative to the child pills.
- Keep the internal stack harmonious: equal horizontal inset, even vertical spacing, centered overall composition within each container, and a calm, balanced rhythm.

Relationships:
- Draw one clean connector from each left-side pill into the NLP grouping block.
- Draw exactly one clean connector from the NLP grouping block into each topic container.
- Keep all lines smooth, softly curved, uncrossed, and visually restrained.
- Incoming lines stay neutral. Outgoing lines pick up the blue or teal family of the destination topic.

Style:
- Prioritize softness, balance, and restraint over generic UI chrome.
- Use tinted containers and subtle elevation to create hierarchy instead of heavy outlines or bright fills.
- The result should feel like a polished editorial product illustration, not a dashboard widget or placeholder graphic.

Avoid:
- No oversized center engine.
- No shared outer frame around both topics.
- No mixed pill radii, mismatched chip styles, or inconsistent widths inside a topic container.
- No literal copying of any reference image.
